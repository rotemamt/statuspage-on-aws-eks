# מתווה מצגת — Status-Page on AWS EKS

עקרון מנחה: כל סלייד = כותרת + עד 3-4 בולטים קצרים + דיאגרמה אם רלוונטית. אם משהו דורש יותר משפט להסביר — זה **דיבור**, לא סלייד. לכל סלייד יש "מה בעל פה" — זה לא כתוב על המסך, זה מה שאתה אומר כשעומדים שם.

15 סליידים + מעבר דיבור לדמו (לא סלייד נפרד).

---

## 1. שער
**תוכן:** שם הפרויקט, שמות, תאריך. שום דבר אחר.

## 2. האפליקציה והמטרה
**תוכן:**
- Status-Page = דף uptime/תקריות ציבורי (Django)
- **לא** כתבנו את הקוד — בנינו את התשתית שמריצה אותו
**בעל פה:** אם ישאלו "האם הוא בודק את עצמו" — להסביר את הפרדוקס (מי שומר על השומרים), לא לפני שנשאלים.

## 3. למה בכלל Kubernetes
**תוכן:** החלטה + שורה אחת: *"Overkill לגודל האפליקציה — אבל זה מה שהתעשייה בודקת"*. טבלה מצומצמת ל-**3 שורות בלבד**: שרידות / סקיילינג / ערך שוק.
**בעל פה:** להכיר בכנות ש-K8s "יותר מדי" לפרויקט הזה, ולהסביר שזו החלטה מודעת. שאר שורות ההשוואה (cost/complexity/monitoring) — רק אם נשאלים.

## 4. EKS מול self-managed
**תוכן:** החלטה + **מספר אחד**: $73/חודש, control plane מנוהל ע"י AWS. שלוש מילות מפתח: Multi-AZ אוטומטי, ~שעה הקמה מול ימים.
**בעל פה:** ה-framing המלא ("שקלנו self-managed לערך הלימודי, אבל...") — פה, לא על המסך.

## 5. דיאגרמת הארכיטקטורה המלאה ★
**תוכן:** **רק הדיאגרמה** (`aws-infra-flow.drawio` או הגרסה המעודכנת). כמעט בלי טקסט.
**בעל פה:** הסלייד שעליו מבלים הכי הרבה זמן — כאן מסבירים את כל זרימת הבקשה, שכבות הרשת, מה ציבורי/פרטי.

## 6. עיצוב הרשת
**תוכן:** תרשים VPC (2 AZ, public/private). קריאה אחת מודגשת: **"NAT יחיד — חוסך ~$32/חודש, SPOF ידוע, prod אמיתי היה עם שניים"**.
**בעל פה:** זה בדיוק הסלייד שמראה שאתם יודעים לוותר במודע, לא מפספסים בטעות.

## 7. Node Management
**תוכן:** Managed Node Groups. שורה אחת: *"Fargate/Karpenter — פתרונות לבעיית scale שאין לנו"*.
**בעל פה:** שלוש האופציות (Fargate/Karpenter/MNG) בפירוט — רק אם נשאלים.

## 8. שכבת הדאטה
**תוכן:** RDS Multi-AZ + ElastiCache Multi-AZ — שתיהן עם אותה סיבה: **קריטריון שרידות**. הערה אחת טכנית: cluster-mode disabled (חובה, לא אופציה — 2 DBs ב-Redis).
**בעל פה:** למה לא DB בתוך pod (state ו-disposable לא מתערבבים).

## 9. שלוש שכבות Autoscaling ★
**תוכן:** דיאגרמה/תרשים זרימה: **HPA** (web, CPU) → **KEDA** (worker, עומק תור) → **Cluster Autoscaler** (nodes). בלי טקסט נוסף.
**בעל פה:** למה worker לא סקיילינג לפי CPU (idle/IO-bound), ההבדל בין שכבת פוד לשכבת node.

## 10. תיקוני סבב-2 מהמנטור — הוכחת איטרציה
**תוכן:**
- Scheduler: `strategy: Recreate` — למה (singleton, לא כפילות jobs)
- `topologySpreadConstraints` — פיזור בין nodes/AZ
**בעל פה:** תמסגרו את זה כ**"תפסנו את זה בסבב review, לא בפרודקשן"** — זו הנקודה החזקה כאן, לא הפרטים הטכניים.

## 11. זרימת Secrets
**תוכן:** דיאגרמת חץ אחת: Secrets Manager → ESO (IRSA) → K8s Secret → pod. שורה: *"בלי מפתחות סטטיים, בלי סיסמה ב-git"*.
**בעל פה:** rotation, IRSA מול RDS IAM auth (למה לא — לא שולטים בקוד).

## 12. CI/CD
**תוכן:** דיאגרמת ה-summary (`cicd-pipeline-summary.drawio`). שורה אחת: GitHub Actions, לא Jenkins (שרת נוסף שהיינו מחזיקים), לא Argo CD (תקורה שלא משתלמת בקנה מידה שלנו). **בולט נוסף, לא סלייד נפרד:** *"Status-Page ארכיון מ-2025 — סריקת ה-image כאן היא בדיוק המיטיגציה לזה"*.
**בעל פה:** OIDC בלי secrets מאוחסנים, RBAC מצומצם ל-deploy role. שאר המיטיגציות (non-root, network policies, גרסה מוצמדת) — בעל פה אם נשאלים "עוד מה עשיתם בשביל זה".

## 13. עלויות — המספרים האמיתיים ★
**תוכן:** טבלה קצרה בלבד: NAT $32 · EKS $73 · nodes ~$60 · RDS/Redis Multi-AZ · ALB $16 → **סה"כ מול $300**. שום דבר מעבר לטבלה.
**בעל פה:** אם נשאלים על אופטימיזציות שנשקלו — Graviton (t4g) כאופציה זמינה; Spot ל-nodes נבדק ונדחה כי החיסכון בקנה המידה שלנו זניח — **רק אם נשאלים במפורש**, לא ביוזמתכם.

## 14. מה היינו משנים בפרודקשן אמיתי
**תוכן:** 3 בולטים: NAT יחיד (SPOF) · אין GitOps · חלק מה-deploy עדיין ידני.
**בעל פה:** זה הסלייד שמראה בגרות — "אנחנו יודעים בדיוק מה ויתרנו עליו ולמה".

*(מעבר דיבור, לא סלייד: "בואו נראה את זה רץ" → דמו חי)*

## 15. סיכום
**תוכן:** 3 בולטים מקסימום:
- דפוסי production אמיתיים בתקציב $300
- שרידות מובנית מההתחלה (Multi-AZ בכל שכבה)
- החלטה מודעת בכל שלב — כולל **מתי לא** להוסיף מורכבות (Karpenter/Argo CD/GitOps)

---

## מה **לא** נכנס לשום סלייד — בעל פה בלבד, אם נשאלים

- כל האנלוגיות (nginx=דלפק קבלה, cache=פתק דביק, וכו') — טובות להסבר חי, לא לקריאה מהמסך.
- ה-glossary המלא.
- "שש שאלות הכשל" — **אל תבטיחו אותן על סלייד** אם עוד לא עברתם עליהן בפועל מול הקלאסטר החי. אם נשאלים "מה קורה אם X מת" — לענות בעל פה מהידע, לא להצביע על סלייד ריק.
- כל סיפור הדיבוג שלנו עם הרשאות IAM (`GetPolicyVersion`, access-entry churn וכו') — זה סיפור בנייה פנימי, לא רלוונטי למנטור. אם נשאלים "איך התמודדתם עם קשיים" — עונים בעל פה בקיצור, לא סלייד.
- טבלאות ההשוואה המלאות (K8s-מול-VMs, EKS-מול-self-managed) — הגרסה המלאה נשארת ב-`ARCHITECTURE_NOTES.md` בשביל הכנה אישית, לא למסך.
- חיסכון עלות זניח (למשל Spot ב-$0.50/חודש) — **לא מוזכר כלל אלא אם נשאלים ישירות**, ואז בקיצור ובלי להציג כ"הישג".
