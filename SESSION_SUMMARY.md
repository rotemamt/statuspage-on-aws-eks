# סיכום שיחה — Status-Page, דיאגרמת AWS/EKS

## מה נבנה

דיאגרמת תשתית AWS/EKS ברמת production, למצגת למנטור: [`diagrams/aws-infra.drawio`](diagrams/aws-infra.drawio) (לפתוח ב-draw.io / diagrams.net).

## החלטות תשתית שנעולות

| נושא | החלטה | למה |
|---|---|---|
| Load Balancer | **AWS ALB** דרך AWS Load Balancer Controller | סטנדרט תעשייתי ל-EKS, TLS מ-ACM, פחות תחזוקה עצמית |
| RDS PostgreSQL | **Multi-AZ** (standby + failover אוטומטי) | סרוויוואל הוא קריטריון של המנטור |
| ElastiCache Redis | **Multi-AZ replica** (failover אוטומטי) | אותה סיבה — jobs בתור לא צריכים להיעלם אם ה-node הראשי נופל |
| קבצים סטטיים (CSS/JS) | **S3 + CloudFront**, לא gunicorn | ALB מחליף את nginx בקצה, אז אין יותר מי שמגיש סטטי מקומית |
| EKS | Managed (לא self-managed) | AWS מריץ/מתקן control plane, Multi-AZ אוטומטי, לוח זמנים לא סובל דיבוג control plane |
| Node management | Managed Node Groups | פשוט, צפוי, מתאים לתקציב $300 |
| VPC | VPC עצמי (Terraform), לא Default | שליטה מלאה, production-safe, מרשים יותר למנטור |
| NAT Gateway | NAT יחיד (לא 2) | חוסך ~$32/חודש; SPOF ידוע ומוצהר למנטור |

מקור מלא להחלטות + הסברי הרקע (cache vs DB, למה יש 3 תהליכים, glossary וכו'): [`ARCHITECTURE_NOTES.md`](ARCHITECTURE_NOTES.md).

## זרימת הבקשה המלאה (מה שצריך להיות מסוגל להסביר למנטור)

```
1. משתמש → Route 53: resolve DNS → מקבל IP של ה-ALB
2. משתמש → Internet Gateway → ALB (פורט 443, TLS מ-ACM)
3. ALB → web pod בתוך EKS (gunicorn+Django, פורט 8001)
4. web pod בודק קודם Redis DB1 (cache):
   - HIT → מדלג על ה-DB, עונה מיד
   - MISS → ממשיך לשלב 5
5. (רק ב-miss) web pod → RDS PostgreSQL (:5432) → מקבל שורות בחזרה
   → הערך נכתב חזרה ל-cache (DB1) בשביל הפעם הבאה
6. אם הפעולה דורשת עבודה איטית (למשל שליחת מיילים):
   web pod דוחף job ל-Redis DB0 (תור) בלי לחכות — עונה למשתמש מיד
7. rq worker (תהליך נפרד) מושך את ה-job מ-DB0, מריץ אותו, ומסמן שנגמר
8. scheduler (עותק יחיד! לא 2-3 כמו שאר הפודים) דוחף jobs מתוזמנים ל-DB0 לפי שעון
9. התשובה (HTML) חוזרת באותו נתיב בהיפוך: web pod → ALB → IGW → משתמש
10. רק אחרי שה-HTML כבר הגיע, הדפדפן מבקש בנפרד CSS/JS — ישירות מ-CloudFront/S3,
    לא נוגע בקלאסטר בכלל
```

## עקרונות מפתח שחשוב לזכור בהצגה

- **Cache (DB1) ≠ Queue (DB0)** — cache זה קיצור דרך לקריאה (אפשר למחוק בלי לאבד כלום, רק להאט). queue זה עבודה אמיתית שצריך לבצע (מחיקה = jobs אבודים לצמיתות).
- **DB תמיד לפני cache?** לא — ההפך: cache נבדק **קודם**, DB נשאל רק ב-miss.
- **Fire-and-forget** — כשה-pod דוחף job לתור, הוא לא מחכה לתשובה. התשובה כבר נשלחה למשתמש.
- **Scheduler הוא singleton** — שני עותקים = כל job מתוזמן רץ פעמיים (מיילים כפולים וכו').
- **תשובת HTTP חוזרת באותו חיבור** — אין "חץ נפרד" טכני לתשובה, אבל בדיאגרמה יש חצים דו-כיווניים כדי שזה יהיה ברור ויזואלית.

## תיקוני דיאגרמה שנעשו (ואיך להימנע מהם בבנייה עצמאית)

1. **מיקום לוגי לא תואם למיקום ויזואלי** — Route 53 היה מצויר בצד, למרות שהוא שלב 1 ממש על קו הזרימה בין המשתמש ל-Internet Gateway. תוקן: הוזז לשבת על הקו.
2. **בלבול "מה קורה מתי"** — CloudFront/S3 צוירו כאילו הם חלק מהבקשה הראשונית. בפועל: קורה **אחרי** שה-HTML כבר חזר. תוקן: הוזזו למקום נפרד בדיאגרמה עם הערה מפורשת "שלב 9, אחרי שהעמוד כבר נטען".
3. **חצים חופפים שמסתירים תוויות** — שני חצים ששניהם יוצאים מ-web pod ונכנסים לאותו אייקון בדיוק (cache ו-enqueue, שניהם ל-Redis) התנגשו ויזואלית — נראה רק חץ אחד. תוקן: נקודות כניסה/יציאה שונות סביב כל אייקון כדי שהחצים יתפזרו.
4. **תקשורת חד-כיוונית** — לא היה ברור איך התשובה חוזרת מה-DB/cache למשתמש. תוקן: נוספו ראשי-חץ בשני הכיוונים על כל קטע שיש בו באמת בקשה+תשובה (לא על ה-enqueue, כי זה fire-and-forget אמיתי).
5. **"Platform services" מצויר בטעות בתוך תיבת ה-VPC** — ECR/S3/Secrets Manager/CloudWatch/IAM הם שירותים ברמת region/account, לא בתוך ה-VPC. תוקן: הוצאו מחוץ לתיבה.
